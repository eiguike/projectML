# projectAM

## Local setup

We're using Pipenv to easily manage Python dependencies.

To install it, simply:

```bash
pip install pipenv
```

And then:

- `pipenv install --three`
- `pipenv shell`
- Profit!
